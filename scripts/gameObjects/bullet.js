class Bullet {
    constructor(x, y, angle, speed, duration, playerOwned) {
        this.x = x;
        this.y = y;
        this.angle = angle;
        this.speed = speed;
        this.duration = duration;
        this.playerOwned = playerOwned;
    }

    update() {

    }

    render(viewport, canvas, ctx, displayX, displayY) {

    }
}