class Pickup {
    constructor(x, y) {
        this.x = x;
        this.y = y;
        this.width;
        this.height;
        this.duration
    }

    update() { }
    render(viewport, canvas, ctx, displayX, displayY) { }
    onPickup() { }
}